[package]
name = "toy-executor"
version = "0.1.0"
edition = "2021"
description = "forge lab 05 — single-threaded async executor (kernelspace)"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
kslab = { path = "../kit" }

[profile.release]
opt-level = 2
debug = false
