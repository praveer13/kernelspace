[package]
name = "bpe-tokenizer"
version = "0.1.0"
edition = "2021"
description = "forge lab 03 — byte-level BPE tokenizer (kernelspace)"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
kslab = { path = "../kit" }

[profile.release]
opt-level = 2
debug = false
