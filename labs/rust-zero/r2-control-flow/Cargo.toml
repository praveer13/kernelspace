[package]
name = "rust-zero-r2"
version = "0.1.0"
edition = "2021"
description = "Rust Zero R2 — functions and control flow"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
rust-zero-harness = { path = "../harness" }
