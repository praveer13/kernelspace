[package]
name = "rust-zero-r9"
version = "0.1.0"
edition = "2021"
description = "Rust Zero R9 — practical lifetimes"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
rust-zero-harness = { path = "../harness" }
