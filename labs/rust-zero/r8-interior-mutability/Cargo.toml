[package]
name = "rust-zero-r8"
version = "0.1.0"
edition = "2021"
description = "Rust Zero R8 — interior mutability"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
rust-zero-harness = { path = "../harness" }
