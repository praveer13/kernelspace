[package]
name = "rust-zero-r4"
version = "0.1.0"
edition = "2021"
description = "Rust Zero R4 — borrowing and slices"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
rust-zero-harness = { path = "../harness" }
