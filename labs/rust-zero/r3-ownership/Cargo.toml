[package]
name = "rust-zero-r3"
version = "0.1.0"
edition = "2021"
description = "Rust Zero R3 — ownership and moves"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
rust-zero-harness = { path = "../harness" }
