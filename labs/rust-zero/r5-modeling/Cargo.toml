[package]
name = "rust-zero-r5"
version = "0.1.0"
edition = "2021"
description = "Rust Zero R5 — structs, enums, Option, and Result"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
rust-zero-harness = { path = "../harness" }
